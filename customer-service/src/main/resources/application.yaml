server:
  port: ${APPLICATION_PORT:9091}
spring:
  application:
    name: customer-service

axon:
  axonserver:
    enabled: ${AXONSERVER_ENABLED:true}
    servers: ${AXONSERVER_HOST:localhost}:${AXONSERVER_PORT:8124}